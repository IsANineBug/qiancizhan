# AGENTS.md — 千词斩项目工作规则

> 本文件约束所有在本仓库工作的人与 AI 助手。规格以 [SPEC.md](SPEC.md) 为准，步骤划分以 [PLAN.md](PLAN.md) 为准；本文件规定「怎么干活」。

## 1. 汇报制度（硬性要求）

- **每完成 PLAN.md 的一个步骤，必须停下来向用户汇报，经用户确认后才能开始下一步。** 不得连续执行多个步骤。
- 汇报内容固定包含四项：
  1. 本步骤做了什么（一两句话）；
  2. 验收演示：按 PLAN.md 该步骤的「验收」条目逐条说明实际结果（含关键命令输出摘要或页面现象）；
  3. 偏差：与 SPEC/PLAN 不一致之处（没有就写「无」）；
  4. 下一步预告 + git 提交号。
- 执行中发现需要改动 SPEC 或 PLAN（无论大小），先停下说明理由、征得同意后修改文档并提交，再继续干活；不得「先斩后奏」。

## 2. 计划与边界纪律

- 严格按 PLAN.md 步骤顺序执行，每步只做该步定义的事；不提前实现后续步骤的功能。
- SPEC.md 第 5 节「不做的事情」是硬边界：用户没有明确要求解除前，不做拼写模式、自定义词库导入、忘记密码找回、固定域名/HTTPS、深色模式、App/小程序等任何被排除项。
- 功能以 SPEC 第 4 节验收标准为唯一完成定义：功能没跑通验收条目，不得标记该步骤完成。

## 3. 技术锁定（不随意更换）

- 后端：Node.js + Express + 内置 `node:sqlite`，单进程单命令启动；不引入其他数据库、框架或构建工具。
- 前端：原生 HTML/CSS/JS，无框架、无第三方图表库（统计图用 CSS/SVG）。
- 词书固定为内置三本（高考/四级/六级），数据来自 kajweb/dict 预处理产物 `data/dicts/*.json`。
- 核心规则不可改：艾宾浩斯档位 1/2/4/7/15 天，5 档斩词；复习词先于新词；登录连续 15 次错误锁定 10 分钟。

## 4. 数据与安全纪律

- `server/data.db` 是用户运行时数据：绝不提交进 git、绝不删除或覆盖（测试后如需清库，先征得用户同意）。
- 密码只存加盐哈希；新增任何涉及用户数据的接口都要过登录鉴权。
- 词库预处理脚本（scripts/）必须可重跑且不产生副作用覆盖：失败时报告坏数据数量，不静默丢弃、不中断。

## 5. Git 纪律（对应 SPEC 第 9 节）

- 每步骤完成后提交一次，信息格式：`步骤N: 一句话概括`；SPEC/PLAN/README 的实质修改单独提交。
- 提交身份用本项目局部配置 `IsANineBug <IsANineBug@users.noreply.github.com>`。
- 不配置远程仓库，除非用户明确要求；`node_modules/`、`server/data.db`、原始 zip、`data/raw/` 永不入库。

## 6. 启动与验证

- 开发验证统一用 `npm start`（默认 http://localhost:3000），端口被占时用 PORT 环境变量，不写死其他端口。
- 涉及「明天/到期」的调度逻辑，用人为改写数据库中的到期时间来模拟时间流逝，不修改系统时钟。
