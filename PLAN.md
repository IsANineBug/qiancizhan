# 「千词斩」实现计划（PLAN.md）

> 状态：**待批准**（批准前不写任何代码）
> 依据：[SPEC.md](SPEC.md)（F1–F12，共 12 个功能）
> 前置调研结论：工作目录仅含 SPEC.md，属从零开发；网络可访问 GitHub 与 npm，词库下载无障碍；Node v26.9.0（自带 `node:sqlite`），无需安装数据库。

## 项目最终结构（目标）

```
Zdemo1/
├── SPEC.md / PLAN.md / README.md
├── package.json          # npm start / npm run public
├── server/               # 后端：入口、路由、数据库、调度逻辑
├── scripts/              # 词库预处理脚本（一次性/可重跑）
├── data/dicts/           # 预处理后的三本词书 JSON（入库源）
├── public/               # 前端：HTML/CSS/JS（无框架）
└── server/data.db        # 运行时生成的 SQLite 文件
```

## 分步计划

每一步只做一件事；「验收」即"做完后打开页面能看到什么变化"。步骤 0 无页面，以终端输出为验收。

### 步骤 0：词库获取与预处理（scripts/）
**做**：下载 kajweb/dict 的高考、四级、六级三本词书 zip → 解析 → 清洗（去重、去空释义、统一字段：word/音标/词性/中文释义/例句+例句翻译）→ 生成 `data/dicts/{gaokao,cet4,cet6}.json`，并打印每本书的总词数与样例。
**验收**：终端显示三本书各自的词数（预期：高考约 3500 / 四级约 2600 / 六级约 2300，以实际清洗结果为准）和每本前 3 个词的完整字段；无报错。**页面无变化（尚无页面）。**

### 步骤 1：后端骨架 + 数据库初始化 + 词书入库
**做**：`package.json`（`npm start` 可跑）；Express 服务启动于 3000 端口，监听本机与局域网；`node:sqlite` 建表（用户/词书/单词/学习进度/每日记录/登录失败计数）；启动时自动把 `data/dicts/*.json` 灌入数据库（书已存在则跳过）。此步暂用占位接口。
**验收**：`npm start` 后终端打印监听地址（含局域网 IP）；浏览器打开 http://localhost:3000 → 显示一行占位文字（如「千词斩 · 服务已启动」）；终端可查到三本词书的入库词数。**页面仅占位。**

### 步骤 2：注册 / 登录 / 退出（F1）+ 登录失败限速（F11 配套）
**做**：注册（用户名唯一、密码 ≥6 位、两次确认）、登录、退出；密码加盐哈希；HttpOnly Cookie 会话；未登录访问任何页面重定向登录页；同一账号连续 15 次密码错误锁定 10 分钟（含剩余等待时间提示）。
**验收**：打开 http://localhost:3000 → 看到登录页（含「去注册」切换）；注册后自动进入一个占位首页；退出后再访问首页回到登录页；密码连错 3 次提示错误、第 15 次起提示"已锁定，剩余 X 分钟"。

### 步骤 3：核心 API——今日任务、进度与调度（F6 的服务端部分）
**做**：实现并自测（curl，无页面）：取今日任务（到期复习词全部入队按到期排序 + 新词按每日上限截取）、提交单词结果（答对升档/答错退档，档位映射 1/2/4/7/15 天，过第 5 档标记已斩）、首页统计数、按自然日切换与积压补齐。
**验收**：页面无变化（仍是占位首页）；终端里用 curl 走通完整流程：领任务 → 提交答对 → 第二次请求中该词出现在明日复习队列（可人为把到期时间改成过去时刻模拟"明天"）→ 连续通过 5 档后该词记为已斩。

### 步骤 4：学习页（F4 翻卡 + F5 四选一 + 会话流程）
**做**：学习页界面与状态机：先复习词（四选一）后新词（先翻卡再检验）；四选一干扰项取自同书随机词；答错所选变红/正确项变绿并排到队尾直至答对；顶部进度条；完成后的总结页（本次斩 X / 新学 Y / 错 Z + 回首页按钮）。
**验收**：登录后进入学习页 → 新词显示卡片（单词+音标+🔊 占位），点卡片翻面出现释义和例句，底部「下一张/开始检验」可用；检验环节点错变红且该词稍后在本次队列中再次出现；全部完成后看到总结页，回到首页后"累计斩词"与刚才总结一致。

### 步骤 5：首页（F2）+ 导航框架
**做**：首页显示当前词书、今日新词 N / 复习 M、累计斩词、连续天数、「开始学习」大按钮；任务清零时显示"今日任务已完成 🎉"和「再来一轮复习」；底部导航（首页/词书/统计/设置）+ 简洁明亮风格的主题样式（白底卡片、绿色主题、大按钮）。
**验收**：登录后落在首页 → 看到 F2 全部数据与按钮；点「开始学习」进入步骤 4 做好的学习页；底部四个入口可点（词书/统计/设置此时还是占位页）；空数据新账号显示 N=每日上限、M=0。

### 步骤 6：词书页（F3）
**做**：三张词书卡片（书名、总词数、已斩/学习中/未学、进度条），点卡片切换当前词书（选中标记），进度独立，斩完显示「已斩完 ✅」。
**验收**：底部导航进词书页 → 看到三张卡片与正确计数；点「四级」设为当前 → 回首页，标题与任务变为四级的数据；高考的进度未被带动。

### 步骤 7：发音（F7）
**做**：学习页 🔊 按钮接通有道 `dict.youdao.com/dictvoice`（按设置的口音），失败自动降级浏览器 speechSynthesis；连点不叠加播放。
**验收**：学习/检验页点 🔊 → 联网时听到该词发音；断网点 🔊 仍能听到合成音、无报错弹窗；快速连点不重播叠加。

### 步骤 8：统计页（F8）+ 设置页（F9）
**做**：统计页：累计斩词、连续天数、三书进度条、最近 7 天学习量柱状图（纯 CSS/SVG）；设置页：每日新词数（1–100，默认 20）、口音选择、账号名、退出登录；改动即时生效。
**验收**：统计页图表与数字和实际记录一致；设置页把每日新词改为 5 保存 → 次日首页新词数变为 5（当天任务不变）；退出登录回到登录页。

### 步骤 9：公网隧道与云部署交付物（F11 + F12）
**做**：`npm run public` 脚本（未装 cloudflared 时给友好安装指引）；README 完成：本机/局域网使用、公网隧道步骤、云服务器部署章节（Node 安装、上传、pm2/systemd 常驻、防火墙放行）、Tailscale 备选方案。
**验收**：运行 `npm run public` → 终端打印 trycloudflare.com 网址（环境支持时）；故意不装 cloudflared 跑一次 → 得到中文安装指引而非报错堆栈；README 完整可照做。

### 步骤 10：整体验收
**做**：按 SPEC 第 4 节 F1–F12 逐条自测并修缺陷；手机同 WiFi 实测局域网访问；重启服务验证数据持久化（F10）。
**验收**：对照 SPEC 打勾清单全部通过；重启后进度无损；手机能访问并在学习页正常操作。

## 步骤依赖

0→1→2→3 为串行主线（数据→服务→账号→调度核心）；4、5 依赖 3；6、7、8 相互独立可任意穿插，但都依赖 5 的导航框架；9、10 收尾。

## 风险与预案

- 词书 zip 格式与预期不符 / 字段缺失 → 步骤 0 先解析样例再批量清洗，宁可丢弃个别坏数据并计数报告，不让脚本中断。
- 有道发音接口在部分网络不可达 → 已有 speechSynthesis 兜底（SPEC F7 即此设计）。
- 本机 3000 端口被占 → package.json 提供 PORT 环境变量改端口。
