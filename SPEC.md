# 「千词斩」背单词网页应用 · 需求规格书

> 状态：**待确认**（确认本文件之前不开始写代码）
> 日期：2026-09-18

## 1. 一句话描述

一个本地部署的背单词网页应用：内置高考/四级/六级词库，用「翻卡学习 + 四选一检验」的混合模式背词，按艾宾浩斯间隔重复安排复习，账号数据存本地服务器，换设备登录同一账号可继续。

## 2. 技术方案（已确认）

| 项目 | 决定 |
|---|---|
| 前端 | 原生 HTML/CSS/JS，无框架；简洁明亮风格（白底卡片 + 绿色主题 + 大按钮）；手机浏览器可用 |
| 后端 | Node.js（本机已有 v26），单服务进程，启动命令一条；默认端口 3000 |
| 数据库 | Node 内置 `node:sqlite`（零安装），数据文件为项目内单文件 `server/data.db` |
| 词库 | 预处理自开源仓库 kajweb/dict（有道词典数据），内置 3 本词书：高考约 3500 词、四级约 2600 词、六级约 2300 词（含英文词、音标、词性、中文释义、例句；以预处理实际结果为准） |
| 发音 | 有道在线发音优先（联网时音质好），失败自动降级为浏览器 speechSynthesis 离线播报 |
| 账号 | 用户名 + 密码注册登录；密码明文存储（2026-09-18 用户明确要求变更：便于忘记密码时自行查库；本地自用场景，接受机器被接触时密码可读的风险，覆盖原「加盐哈希」方案）；会话用 HttpOnly Cookie |
| 网络范围 | 三档访问：① 本机 `localhost:3000`；② 局域网（服务监听局域网地址，同一 WiFi 下手机可访问）；③ 可选公网：一条命令启动 Cloudflare 快速隧道（内网穿透），得到临时公网网址（自带 HTTPS），手机流量也能访问，详见 F11 |

## 3. 功能清单

- **F1 注册 / 登录 / 退出**
- **F2 首页（今日任务总览）**
- **F3 词书页（三本词书切换与进度）**
- **F4 学习页 · 新词翻卡**
- **F5 学习页 · 四选一检验**
- **F6 艾宾浩斯复习调度**
- **F7 发音**
- **F8 统计页**
- **F9 设置页**
- **F10 数据持久化**
- **F11 可选公网访问（一键内网穿透）**
- **F12 可选云服务器部署**

## 4. 各功能验收标准

约定格式：「打开/进入 → 能看到什么；操作 → 会发生什么」。

### F1 注册 / 登录 / 退出
- 未登录打开网站 → 显示登录页，含「登录」表单和切到「注册」的入口。
- 注册：填用户名 + 密码（两次确认）→ 成功后自动登录并进入首页。用户名重复 / 密码过短（<6 位）→ 页面给出红字提示，不进入。
- 登录：正确账号密码 → 进入首页；错误密码 → 提示「用户名或密码错误」，停留在登录页。
- 已登录状态下刷新页面、关闭浏览器再开 → 仍是登录态。
- 设置页点「退出登录」→ 回到登录页，再访问首页要求重新登录。

### F2 首页（今日任务总览）
- 登录后打开首页 → 能看到：当前词书名称、今日待学新词数 N、今日到期复习词数 M、累计已斩词数、连续学习天数、一个大大的「开始学习」按钮。
- 今日任务未完成 → 点「开始学习」进入学习页，处理完 N + M 个词。
- 今日任务已清空 → 首页显示「今日任务已完成 🎉」，按钮变为「再来一轮复习」（可选进入，把今日已学词再过一遍）。
- 首页有底部/侧边导航，可进入：首页、词书、统计、设置。

### F3 词书页
- 打开词书页 → 看到 3 张词书卡片：书名、总词数、已斩 / 学习中 / 未学三个计数、一根进度条。
- 点某张卡片 → 将其设为「当前词书」（有选中标记），首页的任务和进度切换为该书。
- 每本词书进度独立：背四级不影响高考的进度。
- 某本书全部斩完 → 该卡片显示「已斩完 ✅」徽章。

### F4 新词翻卡（学习页 · 新词环节）
- 进入新词环节 → 屏幕中央一张卡片：正面是英文单词 + 音标 + 🔊 按钮，顶部显示「新词 3/10」进度。
- 点卡片（或「翻面」按钮）→ 翻转显示：词性 + 中文释义 + 英文例句 + 例句中文翻译。
- 点「下一张」→ 出示下一张新词卡；最后一张的按钮变为「开始检验」。

### F5 四选一检验（学习页 · 检验/复习环节）
- 进入检验 → 屏幕显示单词 + 音标 + 🔊，下方四个中文释义选项（干扰项随机取自同一词书的其他词）。
- 点某个选项 →
  - 答对：选项变绿，短暂提示后自动进入下一个词。
  - 答错：所选变红、正确项变绿，该词**自动排到本次队列末尾重新出现**，直到答对为止；每答错一次计一次错误。
- 顶部进度条显示剩余数量。
- 全部完成 → 显示「本次完成」总结页：本次斩掉 X 词、新学 Y 词、共答错 Z 次，按钮「回到首页」。
- 复习词与新词的出场顺序：**先到期复习词（直接四选一），后新词（先翻卡再检验）**。

### F6 艾宾浩斯复习调度
- 规则：
  - 新词当日学完即进入第 1 档；复习点依次为 **学后 1 天、2 天、4 天、7 天、15 天**。
  - 每个复习点答对 → 升一档；通过第 5 档（15 天）→ 该词标记为「已斩」。
  - 任一复习点答错 → 回退一档，按回退后档位的间隔重新排期（第 1 档答错则回到第 1 档重新数 1 天）。
  - 检验环节内答错的新词只在本次会话内回炉，不影响其后续复习档位。
  - 每天首次打开时计算「到期复习词」，全部进入当日队列，按到期先后排序，不设数量上限。
  - 「今日」按本地时区的自然日切换；隔多天没来，积压的到期词一次性补进队列。
- 验收：今天答对一个新词 → 明天首页复习数 +1，队列中出现该词；连续通过全部档位 → 词书页该词计入「已斩」；中途答错 → 其下次到期日回退。

### F7 发音
- 学习 / 检验界面的单词旁均有 🔊 按钮。
- 点 🔊 → 联网时播放有道发音（口音按设置页所选，默认美音）；无网络时自动改用浏览器语音播报，不弹错误。
- 点按钮有可感知反馈（图标瞬时变化），快速连点不叠加播放。

### F8 统计页
- 打开统计页 → 看到：累计斩词总数、连续学习天数、三本词书各自的进度条、最近 7 天每日学习量柱状图（纯 CSS/SVG 绘制，不引第三方图表库）。
- 「连续学习天数」定义：连续的自然日中每天都有完成学习行为（斩词或复习）。

### F9 设置页
- 打开设置页 → 看到：每日新词数输入框（当前值，默认 20，范围 1–100）、发音口音选择（美音/英音）、当前账号名、「退出登录」按钮。
- 把每日新词数改为 10 保存 → 次日首页的「今日待学新词」变为 10（当天已生成的任务不变）。
- 点「退出登录」→ 效果同 F1。

### F10 数据持久化
- 所有账号、进度、学习记录存服务端 SQLite 文件。
- 验收：背完若干词后重启服务进程（或重启电脑）→ 重新登录，进度、档位、统计与重启前一致。
- 换设备验证：同一局域网内用手机浏览器登录同一账号 → 看到相同进度（尽力支持项，不作为硬性验收）。

### F11 可选公网访问（一键内网穿透）
- 平时不启动公网命令 → 服务只在本机/局域网可用，行为同第 2 档。
- 在项目目录运行 `npm run public`（脚本内部调用 Cloudflare 的 cloudflared 快速隧道）→ 终端打印一个 `https://xxx.trycloudflare.com` 形式的公网网址。
- 用手机开流量（不连家里 WiFi）访问该网址 → 能打开登录页、正常登录、背单词；网址自带 HTTPS。
- 每次重启该命令 → 公网网址会变化（免费快速隧道的预期行为，终端和 README 中说明）。
- 本机未安装 cloudflared → 给出友好提示和各系统安装指引（README 附安装教程），不抛裸报错。
- 停止命令或电脑关机 → 公网网址立刻不可访问；本机和局域网访问不受影响。
- 配套安全措施（公网必备）：同一账号连续 15 次密码错误 → 锁定 10 分钟，期间即使密码正确也拒绝登录并提示剩余等待时间，到期自动解锁。

### F12 可选云服务器部署（可选能力）
- 同一套代码不修改即可部署到云服务器（单核 2G 内存即可满足，见第 8 节容量说明）；本机模式与云服务器模式仅配置不同（监听地址、端口），无功能差异。
- README 提供「云服务器部署」章节：Node 安装、代码上传、进程常驻（如 pm2 或 systemd）、防火墙/安全组放行端口等步骤。
- 部署到云服务器后：任何设备直接访问 `http://服务器IP:端口` 使用（不要求配置域名和 HTTPS，需要 HTTPS 时可自行在服务器前挂 Nginx/Caddy，README 给出指引但不作为验收项）。

## 5. 明确不做的事情（v1 边界）

1. 不做拼写默写模式、听音辨词模式（v1 只有翻卡 + 四选一）。
2. 不做自定义词库：不支持导入/导出词表、手动添加单词、编辑释义。
3. 不做邮箱注册、验证码、第三方登录、忘记密码找回（忘记密码由开发者直接清库处理）。
4. 不做路由器端口转发；公网访问提供两种可选方式：Cloudflare 快速隧道（免费临时网址，见 F11）或云服务器部署（见 F12）；固定网址需自购域名，v1 不配置。
5. 不做深色模式。
6. 不做原生 App / 小程序；只保证手机浏览器访问可用。
7. 不做跟读打分、生词本手动管理、笔记、社交分享。
8. 不做多管理员后台；数据库数据不提供界面化管理。
9. 不做离线使用（服务必须在本机运行才能访问）。

## 6. 交付与启动方式

- 交付物：项目文件夹（前端页面 + Node 后端 + 预处理脚本 + 词库数据）+ README。
- 启动：`npm start`（或等价的一条命令）→ 终端提示访问 http://localhost:3000，并显示局域网访问地址。
- 公网：`npm run public` 启动临时公网网址（行为见 F11）；cloudflared 的安装方法写入 README。
- 云服务器：同一代码按 README「云服务器部署」章节部署（见 F12）。

## 8. 容量与性能预期

- 目标规模：单核 CPU / 2GB 内存的入门云服务器即可流畅运行；预计常驻内存约 200MB，日常请求对 CPU 占用可忽略。
- 三本词书约 8000 词全部载入内存仍在十几 MB 量级；将来词书扩充到十几本亦无压力。
- 本项目为自用规模（个位数用户），未做高并发设计，不承诺承载大量同时在线用户。

## 9. Git 版本管理方案

- 仓库：项目根目录 `Zdemo1/` 即 git 仓库，主分支 `main`。
- 提交者身份：写入**本项目局部配置**（`git config`，不带 `--global`），不影响机器上其他仓库；具体姓名/邮箱由用户提供。
- 远程仓库：v1 不配置远程，仅本地提交（将来推 GitHub 时再加 remote 即可）。
- 提交纪律：
  - 每完成 PLAN.md 的一个步骤即提交一次，提交信息用「步骤号 + 一句话概括」，如 `步骤2: 注册登录与会话`；
  - 不把生成物和运行时数据提交进仓库：`server/data.db`（用户数据库）、`node_modules/`、词书原始 zip 包通过 `.gitignore` 排除；预处理产物 `data/dicts/*.json` **提交**（它是可重建的交付数据，体积可控）；
  - SPEC.md / PLAN.md / README.md 的每次实质修改单独提交。
- 回滚方式：任何一步出问题，`git log` 找到上一个正常步骤提交，`git checkout <提交号> .` 或 `git revert` 回退，不影响更早的成果。
- 词库预处理脚本可重跑，用于将来扩充其他词书，但 v1 不做成界面功能。

## 7. 实施顺序（确认后执行）

1. 后端骨架：SQLite 建表、注册登录、词库数据预处理入库。
2. 学习闭环：翻卡、四选一、会话流程、艾宾浩斯调度（核心，先打通）。
3. 首页 / 词书页 / 统计页 / 设置页。
4. 发音、登录失败限速、公网隧道命令、错误兜底、手机端适配打磨，对照第 4 节逐条自测。

---

**请确认**：以上第 2、4、5 节（技术方案、验收标准、不做的事情）是否符合你的预期？确认后我开始动工；要改的地方直接指出，我更新本文件。
